const util = require("fs");


class JwtTokenCheck{
  
    constructor(){ }

    init(){
        console.log('restAccessPoint init method should add a component to base nodejs server project')
    }

}

module.exports = new JwtTokenCheck();
